{
	"name": "karmakurier.org",
	"version": "0.0.1",
	"private": true,
	"description": "karmakurier website",
	"scripts": {
		"build-app": "npm run build --prefix ./frontend",
		"dev": "concurrently --kill-others --success first \"npm start --prefix ./backend\" \"npm run serve --prefix ./frontend\"",
		"start": "yarn dev"
	},
	"devDependencies": {
		"concurrently": "^5.1.0"
	}
}
